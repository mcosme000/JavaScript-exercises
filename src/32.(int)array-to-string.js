"use strict";

/*
A simple kata, my first.
simply tranform an array into a string, like so:

  transform([4, -56, true, "box"]) => "4-56truebox"
have fun coding!
*/

function toString(arr) {
  arr.filter((a) => {
    a !== null;
  });
}

toString([[4, -56, true, "box"]]);
